name: 📈 Deploy with SSL

on:
  push:
    tags:
      - 'v-*'

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout
      uses: actions/checkout@v4
      
    - name: 🔧 Setup kubectl
      uses: azure/setup-kubectl@v3

    - name: 🔐 Load Kubeconfig
      run: |
        echo "${{ secrets.KUBE_CONFIG_DATA }}" | base64 -d > /tmp/kubeconfig.yaml
        export KUBECONFIG=/tmp/kubeconfig.yaml
        kubectl get nodes

    - name: 🗑️ Очистить старые ресурсы
      run: |
        export KUBECONFIG=/tmp/kubeconfig.yaml
        echo "=== Очищаем старые ресурсы ==="
        kubectl delete namespace ingress-nginx --ignore-not-found=true
        kubectl delete namespace cert-manager --ignore-not-found=true
        sleep 10

    - name: 🚀 Установить Official Ingress-Nginx
      run: |
        export KUBECONFIG=/tmp/kubeconfig.yaml
        
        echo "=== 1. Устанавливаем официальный ingress-nginx ==="
        kubectl apply -f https://raw.githubusercontent.com/kubernetes/ingress-nginx/main/deploy/static/provider/kind/deploy.yaml
        
        echo "=== 2. Ждем запуска ingress controller ==="
        sleep 30
        
        echo "=== 3. Исправляем порты для Kind ==="
        kubectl patch service -n ingress-nginx ingress-nginx-controller --type='json' -p='[
          {"op": "replace", "path": "/spec/type", "value": "NodePort"},
          {"op": "replace", "path": "/spec/ports/0/nodePort", "value": 30080},
          {"op": "replace", "path": "/spec/ports/1/nodePort", "value": 30443}
        ]'
        
        echo "=== 4. Проверяем ingress-nginx ==="
        kubectl get pods -n ingress-nginx
        kubectl get service -n ingress-nginx

    - name: 📦 Install Cert-Manager (Official)
      run: |
        export KUBECONFIG=/tmp/kubeconfig.yaml
        echo "=== Installing Cert-Manager ==="
        
        # Устанавливаем полную версию cert-manager
        kubectl apply -f https://github.com/cert-manager/cert-manager/releases/download/v1.14.4/cert-manager.yaml
        
        echo "⏳ Waiting for cert-manager to be ready..."
        sleep 60
        kubectl wait --namespace cert-manager \
          --for=condition=ready pod \
          --selector=app.kubernetes.io/instance=cert-manager \
          --timeout=300s
        
        echo "=== Проверяем cert-manager ==="
        kubectl get pods -n cert-manager

    - name: 📝 Настроить ClusterIssuer
      run: |
        export KUBECONFIG=/tmp/kubeconfig.yaml
        
        echo "=== Создаем ClusterIssuer ==="
        kubectl apply -f k8s/infrastructure/cluster-issuer.yaml
        
        echo "=== Проверяем ClusterIssuer ==="
        kubectl get clusterissuer

    - name: 🛠️ Deploy Application with SSL
      run: |
        export KUBECONFIG=/tmp/kubeconfig.yaml
        
        echo "=== Creating ConfigMaps ==="
        kubectl create configmap nginx-html --from-file=html/index.html -o yaml --dry-run=client | kubectl apply -f -
        kubectl create configmap nginx-config --from-file=nginx-config/nginx.conf -o yaml --dry-run=client | kubectl apply -f -
        
        echo "=== Deploying Application ==="
        kubectl apply -f k8s/deployment.yaml
        kubectl apply -f k8s/service.yaml
        
        echo "⏳ Waiting for application to start..."
        sleep 20
        
        echo "=== Deploying Certificate ==="
        kubectl apply -f k8s/infrastructure/certificate.yaml
        
        echo "=== Deploying Ingress with TLS ==="
        kubectl apply -f k8s/infrastructure/ingress-tls.yaml
        
        echo "⏳ Waiting for SSL certificate issuance (this may take 2-3 minutes)..."
        sleep 180
        
        # Проверяем статус сертификата
        echo "=== Checking Certificate Status ==="
        kubectl get certificate -A
        kubectl describe certificate clerikc-ru-tls
        
        # Проверяем challenges
        echo "=== Checking ACME Challenges ==="
        kubectl get challenges -A
        
        echo "✅ Application deployed with SSL!"

    - name: 🔍 Диагностика SSL
      run: |
        export KUBECONFIG=/tmp/kubeconfig.yaml
        
        echo "=== 1. Проверяем статус сертификата ==="
        kubectl get certificate -A
        kubectl describe certificate clerikc-ru-tls
        
        echo "=== 2. Проверяем Order и Challenge ==="
        kubectl get orders -A
        kubectl get challenges -A
        
        echo "=== 3. Проверяем секрет с сертификатом ==="
        kubectl get secret clerikc-ru-tls -o jsonpath='{.data.tls\.crt}' | base64 -d | openssl x509 -text -noout | head 20
        
        echo "=== 4. Логи cert-manager ==="
        kubectl logs -n cert-manager -l app.kubernetes.io/name=cert-manager --tail=30
        
        echo "=== 5. Проверяем ingress ==="
        kubectl describe ingress nginx-cicd-demo

    - name: 🧪 Test SSL
      run: |
        export KUBECONFIG=/tmp/kubeconfig.yaml
        echo "Testing SSL deployment..."
        
        echo "=== Testing HTTPS ==="
        if curl -fk https://clerikc.ru; then
          echo "✅ HTTPS works!"
        else
          echo "❌ HTTPS failed, checking resources..."
          kubectl get ingress -A
          kubectl get certificate -A
          kubectl get challenges -A
          kubectl describe certificate clerikc-ru-tls
          kubectl logs -n cert-manager -l app.kubernetes.io/name=cert-manager --tail=20
          exit 1
        fi